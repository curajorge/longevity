---
description: "Search saved protocols"
argument-hint: "<topic, intervention, biomarker, or name>"
---

Search `protocols/` directory:

1. Search filenames with Glob for matches
2. Search file contents with Grep for the query term
3. If multiple matches, list them with: filename, protocol name, date, category, key interventions
4. If one match, show the full protocol
5. If no matches, say so and suggest what's available

User is looking for: $ARGUMENTS
